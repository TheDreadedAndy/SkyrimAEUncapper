/**
 * @file Compare.h
 * @author Andrew Spaulding (Kasplat).
 * @brief Provides min/max macros.
 * @bug No known bugs.
 */

#ifndef __SKYRIM_UNCAPPER_AE_COMPARE_H__
#define __SKYRIM_UNCAPPER_AE_COMPARE_H__

#define MIN(a,b) (((a) < (b)) ? (a) : (b))
#define MAX(a,b) (((a) > (b)) ? (a) : (b))

#endif /* __SKYRIM_UNCAPPER_AE_COMPARE_H__ */
